# Base URL del API (recomendado: /api con proxy/BFF)
VITE_API_BASE_URL=

# Secreto compartido sitio↔API (solo si aplica; idealmente via proxy/BFF)
CATALOG_API_KEY=

# (Legacy / admin app) Si tu frontend admin usa VITE_API_URL, puedes mantenerlo en tu .env local:
# VITE_API_URL=/api
