# URL base de la API (por defecto usa el proxy de Vite: /api)
# Para desarrollo con proxy, deja vacío o no definas esta variable
# VITE_API_URL=/api

# Para apuntar directamente al backend sin proxy:
# VITE_API_URL=http://localhost:5227/api
